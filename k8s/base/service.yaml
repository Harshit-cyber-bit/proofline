---
apiVersion: v1
kind: Service
metadata:
  name: proofline
  labels:
    app.kubernetes.io/name: proofline
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: proofline
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
