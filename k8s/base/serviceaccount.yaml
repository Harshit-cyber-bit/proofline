---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: proofline
  labels:
    app.kubernetes.io/name: proofline
# The app talks to nothing in the Kubernetes API, so it has no business
# carrying a mounted token that a compromised process could use.
automountServiceAccountToken: false
